spring:
  datasource:
    url: ${TEST_CONTAINER_URL}
    username: ${TEST_CONTAINER_USERNAME}
    password: ${TEST_CONTAINER_PASSWORD}
  liquibase:
    change-log: db/changelog/changelog.xml
    default-schema: walletservice
    liquibase-schema: liquibase